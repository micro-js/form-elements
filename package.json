{
  "name": "@f/form-elements",
  "description": "A list of the elements that can have values inside of a form",
  "repository": "git://github.com/micro-js/form-elements.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
